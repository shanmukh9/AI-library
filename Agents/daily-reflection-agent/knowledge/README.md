# Knowledge Notes

Place your private Markdown notes in this folder, then build the local RAG index:

```powershell
python .\scripts\index_knowledge.py
```

Personal notes are ignored by git by default. Keep examples generic if you decide to commit them.
